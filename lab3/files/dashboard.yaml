apiVersion: 1

providers:
  - name: 'prometheus'
    options:
      path: /etc/grafana/provisioning/dashboards

datasources:
  - name: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090